# build output
dist/
.astro/

# generated by scripts/generate-og.mjs (prebuild) from the committed SVG sources
public/og.png
public/favicon-*.png
public/apple-touch-icon.png
public/email-logo*.png

# dependencies
node_modules/

# logs
npm-debug.log*
pnpm-debug.log*

# env
.env
.env.local
.env.*.local

# macOS
.DS_Store

# editor
.vscode/
.idea/
*.swp
*.swo

# off-site brand assets and identity source material, local-only
brand/

# scratch space, local-only: source material, drafts and working notes
/tmp/

# local-only leak-scan wordlist (the terms themselves are confidential)
.leakwords

# local-only working notes and open decisions
TODOS.md

# design handoffs and source material, dropped in by hand. They are binaries and
# archives, and the repository keeps none: the knowledge from them belongs in
# src/lib/, scripts/assets/ and AGENTS.md, not in a tracked zip.
notes/

# Stray CVs and similar personal documents dropped in the working directory.
# They carry private contact details and belong nowhere near a public repo.
/*.pdf
